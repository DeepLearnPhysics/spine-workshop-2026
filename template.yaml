# Load HDF5 files
io:
  reader:
    name: hdf5
    file_keys: DATA_PATH

# Geometry
geo:
  detector: DETECTOR
    
# Build reconstruction output representations
build:
  mode: both
  units: cm
  fragments: false
  particles: true
  interactions: true